# Ядро портала
fastapi>=0.110
uvicorn[standard]>=0.27
openpyxl>=3.1        # чтение .xlsx (методичка, часть форм)
xlrd>=2.0            # чтение старых .xls (формы ФНС до 2015 г.)

# Необязательно, но улучшает облака слов: настоящая лемматизация вместо стеммера
# pymorphy3>=2.0
